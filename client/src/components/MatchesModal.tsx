/*import type { Log } from "./MainPanel";

type MatchesModalProps = {
  logs: Log[];
  projectName: string;
  bestMatches: string[];
};

export default function MatchesModal({
  logs,
  projectName,
  bestMatches,
}: MatchesModalProps) {
  return (
    <>
      <h1>{projectName}</h1>
    </>
  );
}*/
